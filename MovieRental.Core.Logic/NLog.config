<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <targets>
    <target name ="logfile" xsi:type="File" layout="${longdate} ${message} ${exception:format=tostring}" fileName="${basedir}/logs/FilmServiceLog.txt" />

    <target xsi:type="EventLog"
        name="logevent"
        layout="${longdate} ${message} ${exception:format=tostring}}"
        machineName="."
        source="App"
        log="FilmServiceLog" />
  </targets>
  <rules>
    <logger name="eventLogger" minLevel="Trace" writeTo="logevent" />
    <logger name ="FilmServiceLog" minLevel ="Trace" writeTo ="logfile"/>
  </rules>
</nlog>